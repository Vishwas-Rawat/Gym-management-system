spring.application.name=gymmanagement
server.port = 8083
# listen on all network interfaces
server.address=0.0.0.0
# PostgreSQL data source
spring.datasource.url = jdbc:postgresql://localhost:5432/gym_management
spring.datasource.username = postgres
spring.datasource.password = Vishwas@2002
spring.datasource.driver-class-name = org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


logging.level.org.springframework.security=DEBUG

spring.web.cors.allowed-origins=http://192.168.18.107:3000,http://localhost:3000,http://127.0.0.1:3000,http://10.92.18.105:3000,http://192.168.18.107:3000
spring.web.cors.allow-credentials=true

# Allowed methods
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS

# Allowed headers
spring.web.cors.allowed-headers=*

# Exposed headers (optional)
spring.web.cors.exposed-headers=Authorization

# Whether credentials (cookies, auth headers) are allowed
spring.web.cors.allow-credentials=true

# SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=kanharawat56@gmail.com
spring.mail.password=ppnv xpcj dgcq hnlq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true